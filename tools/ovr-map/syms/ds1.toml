# ovr-map EXE symbol catalogue, Dark Sun: Shattered Lands (DS1).
#
# Each [[function]] row names one location in DS1's DSUN.EXE. Names
# render in `ovr-map --disasm` entry separators, `--callgraph` edges
# and `--verify` output when loaded with `--syms syms/ds1.toml`.
#
# Schema (all fields required unless marked optional):
#
#   [[function]]
#   name = "load_resource"       # display name
#   segment = "resident"         # "resident", or an integer overlay segment index
#   offset = 0x58b4              # file offset when resident; segment-local
#                                # cs: offset when overlaid
#   confidence = "verified"      # verified | probable | provisional
#   evidence = "..."             # where the identification is written up
#   dso_source = "DSO::Name"     # optional: DSO v1.0 symbol the name came from
#   notes = "..."                # optional: rationale, falsification conditions
#
# Addressing follows docs/dsun-exe-survey.md: resident functions are
# addressed by file offset; overlay functions by (segment index,
# segment-local cs: offset), the same coordinates as the entry stubs.
#
# Confidence definitions:
#
#   verified     identification cross-checked against behavior or a
#                second independent observable (string ref, call
#                shape, constant); safe to build on
#   probable     located and consistent with all evidence, one
#                confirming observable short of verified
#   provisional  hypothesis under test; must state in `notes` what
#                would confirm or kill it
#
# Curation rule (do not relax without surfacing it):
#
#   Add or change a row only with a written evidence chain in this
#   repo's docs (docs/dsun-exe-re.md, docs/dsun-exe-survey.md, or a
#   fix writeup). Machine-generated proposals from
#   scripts/propose-exe-symbols.py are review input, never commits;
#   every row here is hand-accepted.

[[function]]
name = "load_resource"
segment = "resident"
offset = 0x58b4
confidence = "verified"
evidence = "docs/dsun-exe-re.md 2; docs/dsun-exe-survey.md 3.3 (0001:04a4 = file 0x58b4)"
notes = """
The four-CC resource loader, `load_resource(fourcc, id, far *buf)`. \
Identified by the CMAT/CPAL fallback call shape and the FOURCC push \
census; carries a resident-side call-site census (96 sites, zero \
overlay callers).
"""

[[function]]
name = "overlay_manager"
segment = "resident"
offset = 0x466e0
confidence = "probable"
evidence = "docs/dsun-exe-survey.md 3.2"
notes = """
The Borland VROOM overlay dispatch body (INT 3Fh handler side). \
Bounds how many segments stay resident; directly relevant to the \
Phase 7 elevator race. One confirming observable (a runtime trace \
of a paging event) short of verified.
"""

[[function]]
name = "load_game_from_disk"
segment = 21
offset = 0xdde
confidence = "verified"
evidence = "scripts/xref-string.py: pushes 'Failed Uncompress in Loadgamefromdisk' (file 0x49d94, DGROUP offset 0x1434) at 0x68eae"
dso_source = "DSO::LoadGameFromDisk"
notes = """
The error string names the source function itself \
(Loadgamefromdisk) and this is the containing function: the DS1 \
save-load path.
"""

[[function]]
name = "gpl_disk_change_region"
segment = 21
offset = 0x1487
confidence = "probable"
evidence = "scripts/xref-string.py: pushes 'Fatal error: Region change, Invalid save' (file 0x49e43, offset 0x14e3) at 0x6965f; also pushes ' Failed to move to region #%Fs ' (0x49e70, 0x1510) at 0x696b4"
dso_source = "DSO::GplDiskChangeRegion"
notes = """
Region-change module (gpldisk.c family). Behavioural anchor; the \
exact DSO counterpart name is not yet pinned.
"""

[[function]]
name = "load_teleport"
segment = 21
offset = 0x17d0
confidence = "probable"
evidence = "scripts/xref-string.py: pushes 'Error : Can't load Game data in load teleport' (file 0x49e92, offset 0x1532) at 0x69907"
notes = """
Same gpldisk.c module as gpl_disk_change_region; the teleport path \
shares the save/region code.
"""

[[function]]
name = "save_game_to_disk"
segment = 13
offset = 0x7cc
confidence = "probable"
evidence = "scripts/xref-string.py: pushes 'SAVE%.2d.SAV' (file 0x494e9, offset 0xb89) at 0x6146c and 'Maximum of %ld save games!' (0x494f6, 0xb96) at 0x6150c"
dso_source = "DSO::SaveGameToDisk"
notes = """
The save-slot path: builds the slot filename and enforces the \
save-count cap. Family naming from the DSO table.
"""

[[function]]
name = "gpldisk_ictrl_check"
segment = 21
offset = 0x0
confidence = "probable"
evidence = "scripts/xref-string.py: pushes 'Bad iCtrl in gpldisk.c' (literal with newline, file 0x49d3c, offset 0x13dc) at 0x68039"
notes = """
The segment's first entry. Validates the iCtrl field when loading \
saved games; an internal-consistency assert of the gpldisk.c \
module.
"""

[[function]]
name = "awaken_game"
segment = 4
offset = 0x165
confidence = "probable"
evidence = "scripts/xref-string.py: pushes 'could not awaken game' (file 0x48e84, DGROUP offset 0x524) at 0x568b3"
notes = """
The awaken-from-save path (DARKSAVE.GFF to DARKRUN.GFF); the 'cannot reopen darkrun in awaken game' and 'GUI ERROR in awaken' strings belong to the same module.
"""
